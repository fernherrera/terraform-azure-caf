This folder contains Terraform variable definition files and backend configuration files for use via the command line with the -var-file and -backend-config options.

** NOTE **
The files should not have the traditional .tfvars or .tfvars.json extension so that they will not be auto loaded.